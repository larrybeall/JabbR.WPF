<UserControl x:Class="Jabbr.WPF.Messages.ChatMessageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:props="clr-namespace:Jabbr.WPF.AttachedProperties"
             xmlns:converters="clr-namespace:Jabbr.WPF.Converters">
    <UserControl.Resources>
        <converters:HtmlToXamlConverter x:Key="htmlToXamlConverter" />
        <converters:DebugConverter x:Key="debugConverter" />
        <converters:DateTimeConverter x:Key="dateTimeConverter" />
    </UserControl.Resources>
    <Border BorderThickness="0 1 0 0" >
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="180" />
                <ColumnDefinition />
                <ColumnDefinition Width="95" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Image />
                <TextBlock Text="{Binding User.Name}" />
            </StackPanel>
            <TextBlock Grid.Column="1" props:TextBlockHelper.XamlString="{Binding RichContent}" />
            <TextBlock Grid.Column="2" Text="{Binding MessageDateTime, Converter={StaticResource dateTimeConverter}, ConverterParameter='T'}" />
        </Grid>
    </Border>
</UserControl>
