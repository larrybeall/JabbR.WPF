<UserControl x:Class="Jabbr.WPF.Messages.LobbyMessageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:converters="clr-namespace:Jabbr.WPF.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <converters:DateTimeConverter x:Key="datetimeConverter" />
    </UserControl.Resources>
    <Border BorderThickness="0 0 0 1" BorderBrush="LightGray">
        <Grid Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="75" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" x:Name="Content" />
            <TextBlock Grid.Column="1" Text="{Binding MessageTime, Converter={StaticResource datetimeConverter}, ConverterParameter=T}" />
        </Grid>
    </Border>
</UserControl>
