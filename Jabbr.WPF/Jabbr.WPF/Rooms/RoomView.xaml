<UserControl x:Class="Jabbr.WPF.Rooms.RoomView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:metro="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:users="clr-namespace:Jabbr.WPF.Users"
             xmlns:converters="clr-namespace:Jabbr.WPF.Converters">
    <UserControl.Resources>
        <converters:GroupTypeConverter x:Key="groupTypeConverter" />
        <converters:StringFormatConverter x:Key="formatConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"  />
            <RowDefinition />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition MaxWidth="400" MinWidth="200" />
        </Grid.ColumnDefinitions>

        <!-- row 0 -->
        <TextBlock Grid.Column="0" Grid.Row="0" FontSize="32" Text="{Binding RoomName, Converter={StaticResource formatConverter}, ConverterParameter='#{0} Messages'}" />
        <TextBlock Grid.Column="1" Grid.Row="0" FontSize="32" Text="Users" />

        <!-- row 1-->
        <ItemsControl
            ItemsSource="{Binding Messages}"
            Grid.Row="1"
            Grid.Column="0">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel VerticalAlignment="Bottom" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <ContentControl cal:View.Model="{Binding}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <ItemsControl
            Grid.Row="1"
            Grid.Column="1"
            ItemsSource="{Binding Users}">
            <ItemsControl.GroupStyle>
                <GroupStyle>
                    <GroupStyle.ContainerStyle>
                        <Style TargetType="GroupItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="GroupItem">
                                        <Expander IsExpanded="True">
                                            <Expander.Header>
                                                <TextBlock FontSize="18" Text="{Binding Name, Converter={StaticResource groupTypeConverter}}"/>
                                            </Expander.Header>
                                            <ItemsPresenter />
                                        </Expander>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </GroupStyle.ContainerStyle>
                </GroupStyle>
            </ItemsControl.GroupStyle>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <users:UserView cal:View.Context="{Binding}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>

        <!-- row 2 -->
        <Border HorizontalAlignment="Stretch" Grid.Column="0" Grid.Row="2" Background="{DynamicResource BlackBrush}">

        </Border>
        <Border HorizontalAlignment="Stretch" Grid.Column="1" Grid.Row="2" Background="{DynamicResource BlackBrush}">

        </Border>
    </Grid>
</UserControl>
